# Email Configuration# Example environment variables for local development or to document deployment requirements.

EMAIL_USER=your-email@gmail.com# For production builds (hosted over HTTPS) you MUST set VITE_API_BASE to the

EMAIL_PASSWORD=your-app-specific-password# backend API URL so the frontend doesn't POST to the static host and receive

# 405 responses.

# Default dive center email (fallback if dive center has no email)# Example (replace with your backend service URL):

DEFAULT_DIVE_CENTER_EMAIL=divecenter@example.comVITE_API_BASE=https://<your-backend-service>.up.railway.app



# Database Configuration (optional - defaults in code)# Optional: override the dev host for local testing (uncomment and edit)

DB_HOST=localhost# VITE_API_BASE=http://192.168.1.50:4000

DB_USER=root
DB_PASSWORD=12345678
DB_NAME=padi
