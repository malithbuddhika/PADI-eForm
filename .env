# Email Configuration - For localhost testing, emails will be logged to console
EMAIL_USER=test@localhost.local
EMAIL_PASSWORD=test-password

# Default dive center email (fallback if dive center has no email)
DEFAULT_DIVE_CENTER_EMAIL=divecenter@localhost.local

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=12345678
DB_NAME=padi

# API Configuration for frontend
# For localhost development, this can be empty or http://localhost:4000
VITE_API_BASE=http://localhost:4000

# Email Mode - set to 'test' for localhost (logs emails instead of sending)
EMAIL_MODE=test

DB_NAME=padi
